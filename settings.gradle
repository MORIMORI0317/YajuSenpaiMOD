pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        gradlePluginPortal()
    }
}
if (JavaVersion.current().ordinal() + 1 < 16) {
    throw new IllegalStateException("申し訳ないがJava16+以外はNG")
}
